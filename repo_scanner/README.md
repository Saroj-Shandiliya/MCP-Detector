# GitHub Repository Code Scanner

A modular tool to scan GitHub repositories and classify them as Server, Client, or Protocol-related.

## Features
- Scans repositories, users, or organizations.
- Detects keywords, dependencies, and AST structures.
- Classifies based on confidence scoring.
- Extensible architecture.

## Usage
```bash
python -m cli.main scan repo owner/repo
```
